const os = require('os');
console.log(os)
console.log(os.type()) //Returns the name of the operating system //linux
console.log(os.version()) //Returns the version os os //Returns the name of the operating system
console.log(os.freemem()) //Returns the
console.log(os.cpus()) //Returns an array containing information about the computer's CPUs
console.log(os.hostname())//Returns the hostname of the operating system
console.log(os.userInfo())

console.log(__dirname)
console.log(__filename)